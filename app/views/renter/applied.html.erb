<!DOCTYPE html>
<html>
<head>
	<style>
  .btns{
    margin-left:5%;
    background-color:#333333;
  }
  .btn2{
  border: none;
  margin-left:40%;
  background-color:#333333;
  border-radius: 5px;
  font-size: 13px;
  padding: 4px 4px;
  color:white;
  cursor: pointer;
  display: inline-block;}
  .center{
    font-size:16px;
  }
  </style>
  <ul>
  <li><%= link_to "RentedList", rentedlistp_path ,style: 'color: #FFFFFF'%></li>
  <li><%= link_to "Wishlist",wishlistp_path, style: 'color: #FFFFFF' %></li>
 <li><%= link_to "ViewAll", view_prop_path ,style: 'color: #FFFFFF'%></li>
  <li style="float:right"><%= link_to "logout", destroy_renter_session_path, method: :delete,style:'color:#FFFFFF' %></li>
  </ul>

	<div class="login-form">
	<div class="sign-in-htm">
<br>
<h1 class="center"> Welcome <%=@renter.name%></h1>

<h1 class="center"><i>Properties that you applied</i></h1>

<% @propertylist.each do |prop|%>

<div class="clearfix">
<div class="box">


<% prop = Property.find(prop.property_id)%>

	<%= link_to "View property", viewproperty_path(prop),class:"btn2"%>
	
		<h2 class="propname"><%= prop.name%><br></h2>
		

	<%(0...prop.images.count+1 - prop.images.count).each do |img| %>
   <a href=""> <%= image_tag(prop.thumbnail(img),class:"br") %></a>
   <%end%>
   		<br><br>
   		<span class="textd">Property Location :</span>
		<span class="borde"><%= prop.address %></span><br>
		<div><span class="textd" >Rent($)</span> &emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&emsp;&ensp;&ensp;<span class="textd">Bedrooms</span></div>
		<div class="rent"><%= prop.rent%></div>

		<div class="bedrooms"><%= prop.bedrooms%></div><br><br>
		<span class="textd">City</span>
		<div class="borde"><%=prop.city%></div>
		<br>
		<span class="textd" >About Property</span>

		<span class="borde"><%= prop.description%></span>
		
		
			</div>
		<div>
		<% end %>
