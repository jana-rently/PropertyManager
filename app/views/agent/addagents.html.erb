
<!DOCTYPE html>
<html>
<head>
	
	<style >
body{
	margin:0;
	color:#6a6f8c;
	background:#808080;
	font:600 16px/18px 'Open Sans',sans-serif;
  background-color: #f2f2f2; 
}

    .codehim-form{
        max-width: 400px;
        min-height: 400px;
        box-sizing: border-box;
        background: rgba(255, 255, 255, 0.6);
        box-shadow: 4px 2px 16px rgba(0, 0, 0, 0.4);
        border-radius: 8px;
        margin:  20px auto 0 auto;
        padding: 25px;
        color: #414141;
            
    }
    
	</style>
<body>
<div style="color:white;margin-left:80%">
<% if @agent.errors.any? %>
  <% @agent.errors.messages.each do |message| %>
    <div class="message"><%= message.last.last.html_safe %></div>
  <% end %>
<% end %>
</div>
<br><br><br>
<div class="makecenter">Create Agent </div>
<br>
<div class="codehim-form">


    <%= form_for :@agent, url:'/agentcreate'  do |f| %>
  					
					<%= f.label :Agent_name ,class: "label"%>
					<%= f.text_field :name, class: "cm-input"%>
		
				<%= f.label :Address, class: "label"%>
				
 				<%= f.text_area :address, class: "cm-input"%>

				<%= f.label :Contact, class: "label"%> 
				
				<%= f.text_field :contact, class: "cm-input"%>

  				<%= f.label :Email , class: "label"%><br/>
  				
    			<%= f.email_field :email, autofocus: true, autocomplete: "email", class: "cm-input" %>
			
            	<%= f.hidden_field :role, value: 'Agent' %>
                 <%= f.hidden_field :company_id, value: current_agent.company_id %>
	<br><br> 
    	<%= f.submit "Create", class: "btn-login gr-bg" %>
      
		<% end %>
</div>	
</div>	


</body>
</html>