<!DOCTYPE html>
<html>
<head>
	
	<style >
body{
	margin:0;
	color:#6a6f8c;
	background:#808080;
	font:600 16px/18px 'Open Sans',sans-serif;
  background-color: #f2f2f2; 
}

    .codehim-form{
        max-width: 400px;
        min-height: 400px;
        box-sizing: border-box;
        background: rgba(255, 255, 255, 0.6);
        box-shadow: 4px 2px 16px rgba(0, 0, 0, 0.4);
        border-radius: 8px;
        margin:  20px auto 0 auto;
        padding: 25px;
        color: #414141;
            
    }

	</style>
<body>
<div style="font-size:10px">
<%= render "agents/shared/error_messages", resource: resource %></div>
<br><br>
<div class="makecenter"> Resend Unlock instructions</div>
<br><br>
<div class="codehim-form">
 
<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
    <div class="group">
  
  <div class="field">
    <%= f.label :email ,class: "label"%><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" , class:"cm-input"%>
  </div>
<br><br>
  <div class="actions">
    <%= f.submit "Resend unlock instructions" ,class: "btn-login gr-bg"%>
  </div>
<% end %>
<br>
<div class="center">
<%= render "agents/shared/links" %>
</div>
</div>